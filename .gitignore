# ── 시크릿 (절대 커밋 금지) ──────────────────────
.env
.mcp.json

# ── 파이썬 ───────────────────────────────────────
__pycache__/
*.py[cod]

# ── 가상환경 ─────────────────────────────────────
.venv/

# ── 데이터·DB (로컬 산출물) ──────────────────────
data/cache/        # 백테스트 데이터 캐시(코드 아님) — data/ 소스 패키지는 추적
*.parquet

# ── 테스트·캐시 ──────────────────────────────────
.pytest_cache/
.ruff_cache/
.cache/

# 워크포워드 실행 결과
tune_results/

# 대시보드 화면 개발 부산물
.playwright-mcp/
