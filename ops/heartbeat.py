"""Heartbeat — 조용한 실패 차단 (10-ops 10.4).

**막으려는 사고**: cron이 안 돌았거나 토큰 발급이 실패해 사이클이 30초 만에 죽으면
DB에 아무 기록도 남지 않는다. "시각이 지났는데 기록이 없다"는 사실 자체가 신호인데,
바깥에서 감시하지 않으면 며칠 뒤 대시보드를 열고서야 알게 된다.

**방식**: 사이클이 정상 종료하면 외부 모니터링(healthchecks.io 등)으로 HTTPS ping을
보낸다. 모니터링 쪽이 예상 시각 + 여유 15분 안에 ping을 못 받으면 알람을 울린다.
우리가 죽어도 바깥이 대신 알아채는 구조라, 우리 코드가 멀쩡할 것을 전제하지 않는다.

**정상 종료와 안전 정지는 다른 주소를 쓴다** — 정지는 즉시 알람, 정상은 안 왔을 때만
알람이다. 이 둘을 한 주소로 합치면 "정지했다"가 "살아 있다"로 읽힌다.

미구현 — 아래 함수는 전부 뼈대다.
"""
from __future__ import annotations


def ping_success() -> None:
    """사이클 정상 종료 신호. 이게 안 오면 모니터링 쪽이 알람을 울린다."""
    raise NotImplementedError


def ping_safe_stop(cause: str) -> None:
    """안전 정지 신호 — 별도 주소. 받는 즉시 알람이다."""
    raise NotImplementedError
